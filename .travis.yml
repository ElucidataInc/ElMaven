os: linux
dist: trusty
language: cpp
compiler: gcc
before_install:
  - sudo apt-get update -y
  - sudo apt-get install g++ -y
  - sudo apt-get install qtdeclarative5-dev qt5-qmake libboost-all-dev -y
script:
  - export QT_SELECT=qt5
  - qmake CONFIG+=release -o Makefile build.pro
  - make -j4
